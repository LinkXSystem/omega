import Logger from '../../src/engine/logger';

describe('SVG', () => {
  it('Function Test Of getCubicBezier', () => {
    Logger.enable('Jest');

    Logger.info('function test !!!');
  });
});
